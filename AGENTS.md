# Agent Instructions

Read these files first:

1. `README.md`
2. `README_AI_AGENTS.md`
3. `LICENSE`
4. `NOTICE.md`

Repository-wide rules:

- Describe this repository as source-available public code, not open source.
- Do not suggest reuse, redistribution, or commercial adoption without checking
  `LICENSE`.
- Describe dependency licensing accurately: project code uses the repository
  license, dependencies keep their own licenses.
- Treat `Rethink Autism, Inc.` and `RethinkFirst` as excluded entities under the
  project license.
- Keep `tests/fixtures/provider_intel/` synthetic. Do not paste third-party page
  captures into the repo.
- Do not commit generated `data/`, `out/`, `storage/`, or other runtime
  artifacts.
- Do not commit proxy credentials, local environment files, or operator-specific
  configs.

For runtime behavior and CLI workflows, follow `README_AI_AGENTS.md` and
`docs/AGENT_OPS_PLAYBOOK.md`.
